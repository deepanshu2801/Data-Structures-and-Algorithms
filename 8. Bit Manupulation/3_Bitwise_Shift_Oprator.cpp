#include <iostream>
using namespace std ; 

int main() 
{
    //left shift << it means if (a << b) =  a * 2^b

    cout<<(4 << 2)<<endl ; 

    //Right shift >> it means if (a >> b) = a / 2^b
    
    cout<<(4 >> 2)<<endl ; 
}